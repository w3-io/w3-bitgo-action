{
  "name": "w3-bitgo-action",
  "version": "0.1.0",
  "private": true,
  "description": "BitGo institutional custody and signing — wallet management, TSS + multi-sig transactions, policy approval workflows, and webhook registration for W3 workflows.",
  "type": "module",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint src/",
    "test": "node --test test/bitgo.test.js test/policy.test.js test/send.test.js",
    "test:live": "BITGO_LIVE_TEST=1 node --test test/live.test.js",
    "all": "npm run format && npm run lint && npm run test && npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@w3-io/action-core": "^0.4.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@vercel/ncc": "^0.38.0",
    "eslint": "^9.39.4",
    "prettier": "^3.4.0"
  }
}
